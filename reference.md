# Reference: шаблоны файлов проекта

Этот файл содержит **только**:
- шаблоны “памяти проекта” (DEV_* / ARCHITECTURE / AGENT_MEMORY),
- правила ведения `DEV_STATE.md` (включая эмодзи/прогресс),
- минимальные договорённости по структуре папок.

Всё инструментальное/учебное вынесено в `tools/*` (экономим токены и убираем дубли):
- **Unity MCP:** [tools/unity-mcp.md](tools/unity-mcp.md)
- **ComfyUI:** [tools/comfyui.md](tools/comfyui.md)
- **Unity Editor:** [tools/unity-editor.md](tools/unity-editor.md)
- **Код и практики (SO, логирование, комментарии):** [tools/code-writing.md](tools/code-writing.md)

## Типичный порядок при этапе

1. **Перед началом задачи:** отметить `[x]` в `DEV_PLAN.md`, добавить задачу в «В процессе» в `DEV_STATE.md`, указать время начала.
2. Внести изменения в скрипты/сцену (файлы в репо или через MCP).
3. При необходимости запросить `editor_state` или `read_console`.
4. **При прогрессе:** обновить описание в «В процессе» (`DEV_STATE.md`), отметить шаги микро-плана, обновить **📈 Прогресс** (фича/проект), при необходимости добавить скриншот.
5. **После завершения задачи:** убрать из `DEV_STATE.md`, добавить запись в `Docs/DEV_LOG/iteration-NN.md` (дата, результат, файлы, скриншот). Обновить «Ближайшие задачи» в `DEV_STATE.md` и **📈 Прогресс**.
6. В ответе указать: что сделано, что проверить в Unity, статус этапа. **Всегда упоминать, что файлы состояния обновлены.**

## Файлы проекта

Все файлы — в корне проекта (или `Docs/`). **При старте новой сессии** агент читает файлы в указанном порядке.

| Файл | Назначение | Размер | Обновление | Чтение при старте |
|------|-----------|--------|------------|-------------------|
| **DEV_CONFIG.md** | Настройки: режим, тогглы, платформа(ы), ориентация, стиль | Маленький | При смене настроек | **1-й (всегда)** |
| **GAME_DESIGN.md** | Дизайн-документ: набросок, механики, экраны, SO | Средний | При утверждении наброска | **2-й** |
| **DEV_STATE.md** | Контекст + в процессе + ближайшие задачи + блокеры + итерация | **Маленький (всегда)** | Каждое действие | **3-й** |
| **AGENT_MEMORY.md** | Долгосрочная память: предпочтения, решения, конвенции, грабли | Маленький–средний | При обнаружении важного | **4-й (всегда, если есть)** |
| **DEV_PLAN.md** | Полный план: все фичи/задачи с чекбоксами | Средний | При планировании | **5-й (при выборе задачи)** |
| **ARCHITECTURE.md** | Архитектура: системы, зависимости, решения | Средний | При начале реализации | **6-й (если есть)** |
| **Docs/DEV_LOG/** | Папка с итерациями: `iteration-01.md`, `iteration-02.md`, ... | Каждый файл маленький | При завершении задачи | По необходимости: **только предыдущую итерацию** |
| **Docs/screenshots/** | Скриншоты по итерациям: `iter-01/`, `iter-02/`, ... | Растёт | При скриншотах | Только по запросу |

### Зачем раздельная структура

- **DEV_STATE.md** — маленький, всегда актуальный. Содержит «Контекст» (2–3 предложения для мгновенного понимания). Не растёт.
- **AGENT_MEMORY.md** — долгосрочная память. Всё, что агент узнал и не должен забывать: предпочтения, решения, конвенции, грабли. Читается при старте.
- **DEV_PLAN.md** — полный план. Читается при планировании, не каждое действие.
- **Docs/DEV_LOG/** — папка с итерациями. Каждая итерация = отдельный файл. Не растёт бесконечно. При старте по умолчанию не читается; **если нужно — читать только предыдущую итерацию**.
- **Docs/screenshots/** — скриншоты разложены по итерациям. Порядок и хронология.

### Правило для агента при старте новой сессии

1. Прочитать `DEV_CONFIG.md` → режим и настройки. **Если нет (старт с нуля):** запросить у пользователя **настройки** и **геймдизайн** (идея игры, механики, экраны). В настройках: режим, платформа(ы), ориентация, стиль, разрешение, input, тогглы (уточняющие вопросы, QA на фичу, поиск готовых решений, ComfyUI, Figma). **Если выбран стиль/UI из Figma** — дополнительно запросить ссылку на файл/фрейм Figma. Использовать структурированный запрос (AskQuestion или явный список). Затем создать `DEV_CONFIG.md` и перейти к наброску. Не стартовать реализацию без запроса настроек и геймдизайна (анти-паттерн).
2. Прочитать `GAME_DESIGN.md` → что за игра. Если нет — после получения настроек и геймдизайна оформить набросок в `GAME_DESIGN.md`.
3. **Планирование** (создание/обновление `DEV_PLAN.md`) выполнять в **Plan mode**: агент формирует план, пользователь подтверждает, затем реализация.
4. Прочитать `DEV_STATE.md` → **секция «Контекст»** даёт мгновенное понимание. Узнать номер итерации N.
5. Прочитать `AGENT_MEMORY.md` → вспомнить предпочтения, решения, конвенции, грабли. Если нет — создать при первой важной записи.
6. **Создать новую итерацию:** `Docs/DEV_LOG/iteration-{N+1}.md` + `Docs/screenshots/iter-{N+1}/`. Обновить номер итерации в DEV_STATE.
7. Прочитать `DEV_PLAN.md` → полный план (если нужно выбрать задачу).
8. Прочитать `ARCHITECTURE.md` → архитектура (если есть).
9. Итерации логов: по умолчанию **не читать**. Если контекста из `DEV_STATE.md` недостаточно — прочитать **только предыдущую** `Docs/DEV_LOG/iteration-NN.md`. Более старые итерации — не читать.

### Поток задач между файлами

```
DEV_PLAN.md          DEV_STATE.md              Docs/DEV_LOG/iteration-NN.md
  [ ] Задача A  ──→   ⚙️ Задача A        ──→   ✅ Задача A (дата, результат)
  [ ] Задача B         (в работе)
  [ ] Задача C         Блокеры: [...]
                       Контекст: [...]
```

1. Задача берётся из `DEV_PLAN.md` → отметить `[x]`.
2. Переносится в `DEV_STATE.md` → секция «В процессе».
3. При завершении → запись в `Docs/DEV_LOG/iteration-NN.md`, убрать из `DEV_STATE.md`.

### Жизненный цикл итерации

```
Новая сессия → DEV_CONFIG → GAME_DESIGN → DEV_STATE (итерация=N) → AGENT_MEMORY
    → Создаём iteration-{N+1}.md → DEV_PLAN → ARCHITECTURE
    → Работаем, пишем задачи в iteration-{N+1}.md
    → Важное открытие → записываем в AGENT_MEMORY
    → Завершаем сессию → «Контекст» в DEV_STATE + «Итог» в iteration-{N+1}.md
```

---

## Шаблон DEV_CONFIG.md

```markdown
# Настройки разработки

Конфигурация проекта. Агент читает этот файл первым при каждой новой сессии.

---

## Режим разработки

**Режим:** [Прототип | Стандартный | Быстрый | Профи]

---

## Опциональные настройки

| Настройка | Значение | Описание |
|-----------|----------|----------|
| Уточняющие вопросы | [включены / отключены] | Задавать ли вопросы перед планом и фичами |
| QA на фичу | [включена / выключена] | Человеческая проверка после каждой фичи (Стандартный/Профи) |
| Автотесты | [включены / отключены / —] | Автоматические тесты (Профи) |
| Поиск готовых решений | [включён / выключен] | Перед реализацией каждой фичи искать готовую библиотеку/пакет/ассет/референс-код (приоритет: библиотека). Если фича слишком маленькая и простая — писать код вручную. |
| ComfyUI | [используется / не используется] | Генерация ассетов через ComfyUI |
| Figma | [используется / не используется] | Макеты UI через Figma (только получение готового дизайна). Если используется — указать **ссылку на файл/фрейм Figma** ниже |
| Ссылка на Figma | [URL или —] | Агент **запрашивает у пользователя** ссылку на Figma, если она не указана и нужна для работы с макетами |

---

## Проект

| Параметр | Значение |
|----------|----------|
| Платформа(ы) | [PC / Mobile / WebGL / ... — одна или несколько через запятую] |
| Ориентация | [портретная / ландшафтная] |
| Разрешение | [1920x1080 / 1080x1920 / ...] |
| Стиль графики | [2D пиксельный / 2D casual / 3D low-poly / ...] |
| Input | [Keyboard+Mouse / Touch / Gamepad / ...] |

---

## Заметки

[Любые дополнительные решения или договорённости с пользователем.]
```

---

## Шаблон GAME_DESIGN.md

```markdown
# Дизайн-документ игры

Утверждённый набросок игры. Агент опирается на этот файл при реализации.

---

## Общее

| Параметр | Значение |
|----------|----------|
| Название | [название игры] |
| Жанр | [жанр] |
| Цель | [1–2 предложения] |
| Платформа(ы) | [PC / Mobile / WebGL / ...] |
| Ориентация | [портретная / ландшафтная] |
| Стиль | [пиксель / cartoon / ...] |

---

## Механики

1. [Механика 1] — [краткое описание]
2. [Механика 2] — [краткое описание]
3. [Механика 3] — [краткое описание]

---

## Экраны / Сцены

| Экран | Описание | Элементы |
|-------|----------|----------|
| MainMenu | Главное меню | Кнопки Play, Settings, Exit |
| Gameplay | Основной геймплей | Персонаж, враги, HUD |
| GameOver | Экран проигрыша | Счёт, кнопка Restart |

---

## Данные (ScriptableObject)

| SO | Назначение | Ключевые поля |
|----|-----------|---------------|
| PlayerData | Параметры игрока | speed, maxHealth, jumpForce |
| EnemyData | Параметры врагов | health, speed, damage |
| GameSettings | Глобальные настройки | difficulty, ... |

---

## Этапы разработки

1. **Этап 1:** [название] — [что сделать]
2. **Этап 2:** [название] — [что сделать]
3. **Этап 3:** [название] — [что сделать]

---

## Заметки

[Дополнительные решения, ограничения, идеи на будущее.]
```

---

## Шаблон DEV_STATE.md

> Этот файл всегда маленький. Только текущая работа + ближайшие задачи + контекст для быстрого старта.

```markdown
# Текущее состояние

**Обновлено:** YYYY-MM-DD HH:MM  
**Итерация:** N

---

## 🧭 Легенда (эмодзи и статусы)

- **Статусы:** 🟦 в работе · 🟨 нужна проверка · 🟥 блокер · 🟩 готово
- **Разделы:** 🧠 контекст · ⚙️ в процессе · 🧩 микро-план · ⏭️ дальше · ⚠️ блокеры · 📸 скрин · 📈 прогресс · 🧾 итерация-лог
- **Метки:** `←` текущий шаг · `[x]` сделано · `[ ]` не сделано

---

## 📈 Прогресс

**Фича (текущая):** XX% `|█████░░░░░|` (K / N шагов микро-плана) · **Статус:** 🟦/🟨/🟥/🟩  \n
**Проект (в целом):** YY% `|██████░░░░|` (M / T задач по DEV_PLAN) · **Статус:** 🟦/🟩  \n

> Правило: **Фича** = микро-план в «В процессе». **Проект** = статистика DEV_PLAN.

## 🧠 Контекст (для быстрого старта)

[2–3 предложения: что делали в последней сессии, где остановились, что дальше. Агент пишет это в конце каждой сессии.]

---

## ⚙️ В процессе

- 🟦 **Фича:** [FeatureName] · **Задача:** [TaskName]  
  [краткое описание]  
  *Начато: YYYY-MM-DD HH:MM*  
  *Прогресс: [что уже сделано]*

  **Микро-план:**
  1. [x] [Шаг, который уже сделан]
  2. [ ] ← [Текущий шаг — на чём сейчас агент]
  3. [ ] [Следующий шаг]
  4. [ ] [Ещё шаг]
  5. [ ] [Финальный шаг: тест / скриншот / итог]

---

## ⏭️ Следующие задачи (3–5 ближайших)

1. [Задача] — [краткое описание]
2. [Задача] — [краткое описание]
3. [Задача] — [краткое описание]

> Полный план → **DEV_PLAN.md**

---

## ⚠️ Известные проблемы / Блокеры

- [Проблема] — [описание, что нужно: решение / ответ пользователя / баг-фикс]

> Если проблем нет — оставить пустым или «Нет».

---

## 📸 Последний скриншот

- **[Описание]** — `Docs/screenshots/iter-NN/[имя].png`  
  *Дата: YYYY-MM-DD HH:MM*

---

## 📊 Общая информация

- **Текущий этап:** N — [название]
- **Этап начат:** YYYY-MM-DD
- **Прогресс этапа:** X / Y задач
- **Итерация:** N (лог: `Docs/DEV_LOG/iteration-NN.md`)
```

---

## Шаблон DEV_PLAN.md

> Полный план проекта. Все фичи и задачи с чекбоксами. Обновляется при планировании.

```markdown
# План разработки

**Создан:** YYYY-MM-DD  
**Последнее обновление:** YYYY-MM-DD HH:MM

---

## Этап 1 — [название]

- [x] Задача 1.1 — [описание]
- [x] Задача 1.2 — [описание]
- [ ] Задача 1.3 — [описание]

## Этап 2 — [название]

- [ ] Задача 2.1 — [описание]
- [ ] Задача 2.2 — [описание]
- [ ] Задача 2.3 — [описание]

## Этап 3 — [название]

- [ ] Задача 3.1 — [описание]
- [ ] Задача 3.2 — [описание]

---

## Статистика

- **Всего задач:** N
- **Завершено:** M
- **Осталось:** N — M

> Подробности выполнения → **Docs/DEV_LOG/**
```

---

## Шаблон AGENT_MEMORY.md

> Долгосрочная память агента. Всё важное, что не должно теряться между сессиями. Читается при каждом старте.

```markdown
# Память агента

> Этот файл — долгосрочная память. Записывай сюда всё важное сразу. Не дублируй DEV_CONFIG / ARCHITECTURE / GAME_DESIGN.  
> Каждые 3–5 итераций — пересмотри и удали устаревшее.

---

## 👤 Предпочтения пользователя

- [YYYY-MM-DD] [Описание предпочтения]

> Стиль работы, что нравится/не нравится, предпочитаемые подходы.
> Примеры: «Предпочитает минималистичный UI», «Не любит корутины — предпочитает async/await», «Просит не создавать пустые MonoBehaviour».

---

## 🔧 Решения и выводы

- [YYYY-MM-DD] [Что пробовали → результат → вывод]

> Что пробовали, что не сработало, почему выбрали конкретный подход.
> Примеры: «NavMesh не подошёл для 2D — перешли на A*», «DOTween быстрее чем встроенные анимации для UI».

---

## 📐 Конвенции проекта

- [YYYY-MM-DD] [Конвенция]

> Именование, структура, паттерны, установленные по ходу работы.
> Примеры: «SO ассеты: XxxData (EnemyData, WeaponData)», «Префабы: PFB_Xxx», «Сцены: SCN_Xxx».

---

## ⚠️ Грабли (gotchas)

- [YYYY-MM-DD] [Проблема → решение/workaround]

> Баги, подводные камни, неочевидное поведение, workarounds.
> Примеры: «Unity MCP теряет связь после Play Mode — перезапустить сервер», «TextMeshPro требует импорт TMP Essentials при первом использовании».

---

## 📝 Важный контекст

- [YYYY-MM-DD] [Контекст]

> Всё остальное: ограничения, планы, договорённости, особенности окружения.
> Примеры: «Проект планируется под мобилку, UI под портрет 1080x1920», «У пользователя слабый ПК — избегать тяжёлых шейдеров».
```

---

## DEV_LOG — папка итераций

> Журнал разбит на итерации. Каждая итерация = одна рабочая сессия (или логический блок работы).
> Файлы хранятся в `Docs/DEV_LOG/`. Не читаются при старте — только по запросу.

### Структура папки

```
Docs/
  DEV_LOG/
    iteration-01.md   ← завершена
    iteration-02.md   ← завершена
    iteration-03.md   ← текущая (или завершена)
  screenshots/
    iter-01/
    iter-02/
    iter-03/
```

### Правила итераций

1. **При старте новой сессии с чистым контекстом:**
   - Прочитать `DEV_STATE.md` → узнать текущий номер итерации N.
   - Создать `Docs/DEV_LOG/iteration-{N+1}.md` (новая итерация).
   - Создать `Docs/screenshots/iter-{N+1}/` (папка для скриншотов итерации).
   - Обновить номер итерации в `DEV_STATE.md`.

2. **В рамках одной сессии:** записи добавляются в текущий файл итерации (append-only, новые сверху).

3. **При завершении сессии:** агент обновляет «Контекст» в `DEV_STATE.md` и добавляет «Итог итерации» в файл лога.

### Шаблон Docs/DEV_LOG/iteration-NN.md

```markdown
# Итерация NN

**Начата:** YYYY-MM-DD HH:MM  
**Завершена:** YYYY-MM-DD HH:MM (или «в процессе»)

---

## Итог итерации

[2–3 предложения: что было сделано за итерацию, ключевые результаты.]

---

## Завершённые задачи

### ✅ [Задача] (Этап N)

- **Описание:** [что было сделано]
- **Файлы:** `Scripts/...`, `Prefabs/...`
- **Результат:** [итог]
- **Скриншот:** `Docs/screenshots/iter-NN/[имя].png` (если есть)

---

### ✅ [Задача] (Этап N)

- **Описание:** [что было сделано]
- **Файлы:** `Scripts/...`
- **Результат:** [итог]

---

> Записи добавляются сверху (новые — выше старых).
```

---

## Шаблон ARCHITECTURE.md (Стандартный / Профи)

````markdown
# Архитектура проекта

Ключевые архитектурные решения. Агент следует этому файлу при реализации.

---

## Системы

| Система | Описание | Ключевые классы |
|---------|----------|-----------------|
| Core | Менеджеры, загрузка сцен | GameManager, SceneLoader |
| Combat | Боевая система | BattleManager, HealthComponent |
| UI | Интерфейс | UIManager, HUDController |
| Data | ScriptableObject данные | PlayerData, EnemyData |

---

## Зависимости между системами

```
Core → (загружает) → Combat, UI
Combat → (читает) → Data
UI → (слушает события) → Combat, Core
```

---

## Ключевые решения

| Решение | Выбор | Почему |
|---------|-------|--------|
| Доступ к менеджерам | [Singleton / ServiceLocator / DI] | [причина] |
| Связь между системами | [Events / Observer / Direct] | [причина] |
| Загрузка ассетов | [Resources / Addressables / Direct] | [причина] |
| Сохранения | [JSON + persistentDataPath / ...] | [причина] |

---

## Структура папок

```
Assets/
├── _source/              # всё созданное агентом/пользователем
│   ├── Scripts/
│   │   ├── Core/
│   │   ├── Combat/
│   │   └── UI/
│   ├── Data/             # ScriptableObject ассеты
│   ├── Prefabs/
│   ├── Scenes/
│   └── Art/
└── ...                   # сторонние ассеты/пакеты (Assets часто захламлён ими)
```

---

## Заметки

[Ограничения, технический долг, планы рефакторинга.]
````

## ScriptableObject (кратко)

Правило: **все настройки и данные — в ScriptableObject ассетах** (`Assets/_source/Data/`).  
Подробности, примеры и anti-patterns: [tools/code-writing.md](tools/code-writing.md).

Минимальный шаблон класса SO:

```csharp
using UnityEngine;

[CreateAssetMenu(fileName = "NewData", menuName = "GameData/NewData")]
public sealed class NewData : ScriptableObject
{
    [Tooltip("Отображаемое имя (для UI).")]
    public string displayName;
}
```

---

## Рекомендуемая структура папок Assets

```
Assets/
  _source/     # всё созданное агентом/пользователем
    Art/
    Audio/
    Data/      # SO ассеты
    Prefabs/
    Scenes/
    Scripts/
    Materials/
    Textures/
    UI/
    Resources/ # Unity Resources (если осознанно нужно)
  ...          # сторонние ассеты/пакеты

Docs/          # Вне Assets (корень проекта)
  DEV_LOG/     # iteration-NN.md
  screenshots/ # iter-NN/
```

Подробности и примеры под режимы: [tools/unity-editor.md](tools/unity-editor.md).

---

## Инструменты и типовые проблемы (ссылки)

Чтобы не дублировать и не раздувать контекст, подробные гайды и troubleshooting вынесены в `tools/*`:

- Unity MCP (паттерны, инструменты, диагностика): [tools/unity-mcp.md](tools/unity-mcp.md)
- ComfyUI (воркфлоу, промпты, Import Settings, fallback): [tools/comfyui.md](tools/comfyui.md)
- Unity Editor (папки, сцены, проверка в PlayMode): [tools/unity-editor.md](tools/unity-editor.md)
- Код/SO/логирование/комментарии: [tools/code-writing.md](tools/code-writing.md)

---

## Ссылки

- [CoplayDev/unity-mcp](https://github.com/CoplayDev/unity-mcp) — пакет установлен в проект; сервер запускается в Unity (Window → MCP for Unity → Start Server).
- Настройка MCP и практики: см. `tools/*` и [SKILL.md](SKILL.md).
- Готовые промпты: [PROMPTS.md](PROMPTS.md).
- Выбор режима: [MODE_CHOICE.md](MODE_CHOICE.md).
